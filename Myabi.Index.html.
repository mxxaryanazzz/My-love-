<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Valentine's Day!</title>
    <style>
        /* Base Styles */
        body {
            background-color: #ffe6e6; /* Light pink background */
            font-family: Arial, sans-serif;
            text-align: center;
            padding: 50px;
            margin: 0;
        }
        h1 {
            color: #ff3366; /* Pinkish color for the title */
            font-size: 2.5em;
        }
        p {
            font-size: 1.2em;
            color: #ff6666; /* Light red color */
        }
        .heart {
            color: red;
            font-size: 4em;
        }
        .button {
            background-color: #ff3366; /* Button color */
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1.2em;
            cursor: pointer;
            border-radius: 5px;
        }
        .button:hover {
            background-color: #ff1a3d; /* Hover effect */
        }

        /* Responsive Styles for Tablets */
        @media screen and (max-width: 768px) {
            body {
                padding: 20px;
            }
            h1 {
                font-size: 2em; /* Slightly smaller title */
            }
            p {
                font-size: 1em; /* Adjust text size for better readability on tablets */
            }
            .heart {
                font-size: 3.5em; /* Slightly smaller heart */
            }
            .button {
                font-size: 1em; /* Make the button slightly smaller */
                padding: 8px 16px;
            }
        }

        /* Further adjust for small screens (mobile) */
        @media screen and (max-width: 480px) {
            h1 {
                font-size: 1.8em; /* Smaller title for mobile */
            }
            .heart {
                font-size: 3em; /* Even smaller heart */
            }
            .button {
                font-size: 0.9em; /* Adjust button size for mobile */
            }
        }
    </style>
</head>
<body>
    <h1>Happy Valentine's Day!</h1>
    <p>Wishing you a day filled with love, happiness, and joy.</p>
    <div class="heart">❤️</div>
    <p>Click the button to send love:</p>
    <button class="button" onclick="alert('Sending you love! ❤️')">Send Love</button>
</body>
</html>
